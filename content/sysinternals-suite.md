# Windows Sysinternals Suite 활용

Windows 환경에서 부하 테스트를 수행할 때, 기본 작업 관리자만으로는 부족한 세밀한 지표를 확인하기 위해 Sysinternals Suite는 필수적인 도구입니다. 마이크로소프트에서 공식 제공하는 이 툴킷은 프로세스, 메모리, 네트워크의 내부 동작을 심도 있게 모니터링할 수 있게 해줍니다.

부하 테스트 및 서버 관리 시 가장 핵심적으로 사용하는 도구들을 정리해 드립니다.

## 1. Process Explorer (작업 관리자의 강력한 상위 호환)

JMeter 부하 발생기나 서버의 리소스를 실시간으로 분석할 때 사용합니다.

- **Handle/DLL 확인**: 프로세스가 열고 있는 파일, 포트, 레지스트리 키를 확인하여 **자원 누수(Leak)**를 찾을 수 있습니다.
- **상세 CPU/Memory 지표**: 단순 점유율 외에도 Private Bytes, Working Set 등 실제 물리/가상 메모리 사용량을 상세히 보여줍니다.
- **TCP/IP 탭**: 특정 프로세스(JMeter 등)가 현재 몇 개의 커넥션을 맺고 있는지 실시간으로 확인 가능합니다.

## 2. TCPView (네트워크 포트 점유 확인)

앞서 언급한 "Address already in use" 에러 대응 시 가장 유용한 툴입니다.

- **실시간 연결 모니터링**: 어떤 프로세스가 어떤 로컬 포트를 사용하여 서버와 통신하는지 한눈에 보여줍니다.
- **TIME_WAIT 확인**: 테스트가 종료된 후에도 포트가 TIME_WAIT 상태로 묶여 있는지 시각적으로 확인하여 클라이언트 포트 고갈 원인을 진단합니다.

## 3. Process Monitor (Procmon)

시스템에서 발생하는 모든 파일, 레지스트리, 네트워크 활동을 로그 형태로 기록합니다.

- **디버깅**: JMeter가 설정 파일을 읽지 못하거나, 권한 문제로 CSV 데이터를 못 불러올 때 어떤 경로에서 Access Denied가 발생하는지 추적할 수 있습니다.
- **병목 지점 확인**: 특정 프로세스가 파일 I/O를 과도하게 일으키고 있지 않은지 확인하는 용도로 씁니다.

## 4. RAMMap (물리 메모리 상세 분석)

Windows가 메모리를 어떻게 할당하여 사용 중인지 물리적 구조를 보여줍니다.

- **캐시 관리**: 테스트 중 Windows의 File System Cache가 너무 비대해져서 정작 JMeter가 쓸 메모리가 부족해지는 현상을 분석할 때 유용합니다.
- **Empty 기능**: 테스트 전후로 워킹셋이나 대기 목록(Standby list) 메모리를 강제로 비워 깨끗한 상태에서 테스트를 시작할 수 있게 도와줍니다.

## 💡 실전 활용 팁 (t3.medium 서버/클라이언트 관리)

- **부하 발생기(Windows PC) 최적화**: 테스트 시작 전 RAMMap으로 대기 메모리를 정리하고, 실행 중에는 TCPView를 띄워 포트 사용량이 6만 개 근처까지 가는지 확인하세요.
- **Scouter와 병행**: Scouter는 자바 앱 내부(JVM)를 잘 보여준다면, Process Explorer는 윈도우 OS 전체 관점에서 부하 발생기 자체가 지치지 않았는지(Context Switch 등)를 체크하는 데 사용합니다.